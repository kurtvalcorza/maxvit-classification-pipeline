# Release status

Current status: **Candidate — source complete, snapshot pinned, no execution recorded**. The pipeline package, the offline test suite (including a shrunken random-weight `maxvit_tiny_tf_224` architecture run through prediction, the zero-shot baseline, full and frozen fine-tuning, evaluation and adapter reload), `MODEL_CARD.md` (MODEL_CARD_SPEC 1.2) and the standalone `E2E` tutorial `tutorials/maxvit_classification_colab.ipynb` (generated by `tools/build_notebook.py`, DIMER Notebook Specification 2.1) exist. The upstream snapshot is pinned: `MODEL_REVISION` is `041f2cce4d74c7539d63aa9fb85786e78072d487` and the manifest records every file's byte size and SHA-256 (`model.safetensors` `e3998ec8e5f7…`, matching the Hub's LFS record). No execution with the pinned weights is recorded in `docs/release-verification.md`, so the repository is not release-grade.
