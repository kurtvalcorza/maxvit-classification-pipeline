# Release status

Current status: **Candidate — default-path execution recorded on 2026-09-26 (Kaggle T4); REL12 BYOD exercise pending before promotion**. The pipeline package, the offline test suite (including a shrunken random-weight `maxvit_tiny_tf_224` architecture run through prediction, the zero-shot baseline, full and frozen fine-tuning, evaluation and adapter reload), `MODEL_CARD.md` (MODEL_CARD_SPEC 1.2) and the standalone `E2E` tutorial `tutorials/maxvit_classification_colab.ipynb` (generated by `tools/build_notebook.py`, DIMER Notebook Specification 2.1) exist. The upstream snapshot is pinned: `MODEL_REVISION` is `041f2cce4d74c7539d63aa9fb85786e78072d487` and the manifest records every file's byte size and SHA-256 (`model.safetensors` `e3998ec8e5f7…`, matching the Hub's LFS record). The exact commit `0c1164e` / notebook blob `b736baab41ad` executed top-to-bottom on Kaggle Tesla T4 on 2026-09-26 UTC with both BYOD branches off (322.8 s, 14/14 post-restart code cells): on 60 held-out CIFAR-10 thumbnails the fine-tuned head scored accuracy 1.000, and the zero-shot ImageNet mapping also scored 1.000 (untrained head 0.367, majority baseline 0.500), so this run shows no gain from fine-tuning. The tutorial split is not aware of the sample archive's 100 pixel-identical groups: by the IDs in `maxvit_classification_predictions.csv`, 46 of 60 held-out and 36 of 60 unseen images have their same-numbered counterpart (`original_images` ↔ `darkened_images`) outside every evaluation split, that is, in train (a count of counterparts, not of confirmed pixel copies). The full record is in `docs/release-verification.md`. Release step 7 (REL12: the BYOD dataset branch with a small class-folder directory and with an incompatible one) has not been exercised, so the status stays Candidate.
