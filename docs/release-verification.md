# Release verification

`tutorials/maxvit_classification_colab.ipynb` (`E2E`, **standalone** carrier) is a **release candidate** until the exact notebook revision has executed top-to-bottom in a clean supported runtime. Unit tests, the tiny-model test, JSON validation, code-cell compilation, the generator parity checks and `tools/validate_release_assets.py` are necessary checks but are **not** runtime evidence under DIMER Notebook Specification 2.1 (REL8). This file is the durable release-gate record for the notebook.

The upstream snapshot is pinned to `041f2cce4d74c7539d63aa9fb85786e78072d487` and the notebook is regenerated with that revision and manifest, so it can run. One default-path execution is recorded below (2026-09-26, Kaggle T4); release step 7 (REL12) has not been exercised, so the status stays `Candidate`.

## Automatic coverage (static and unit, every pull request)

CI installs the pinned CPU-only torch wheel and the other runtime pins, then runs:

- `ruff check src tests tools`;
- `pytest`: snapshot verification and staging against synthetic manifests, the unpinned refusals, the import boundary, input and dataset validation, BYOD directory reading, average precision, the evaluation report, the notebook parity checks, the weight-facts check, the pin tool against a fake Hub, the data module against synthetic archives and folders (member-path refusals, size bounds, digest verification, stratified splits), and `tests/test_small_model.py` — a shrunken random-weight `maxvit_tiny_tf_224` architecture taken through `predict`, `zero_shot_evaluate`, full and frozen `finetune` (including the check that a frozen backbone keeps its weights and BatchNorm statistics), `evaluate`, `save_artifact` and `apply_artifact`;
- `tools/validate_release_assets.py`: model card 1.2 structure and front matter, the pin state across the package, the manifest and the documents, identity consistency, the weight facts, the release-status tokens, and the notebook's structure, carried modules, parity, markers, BYOD gates and location fields;
- `tools/build_notebook.py --check`.

These are source, provenance and unit checks. None of them loads the pinned checkpoint, so none of them is execution evidence.

## Supported release verification procedure

Before changing the registry status from `Candidate` to `Release-grade`:

1. confirm the snapshot is pinned (`MODEL_REVISION` is a 40-hex commit and every manifest entry has a SHA-256) and that static CI is green on the exact commit under review;
2. open that exact notebook revision in a new GPU runtime (Colab or Kaggle, Tesla T4 or similar) with **no repository checkout** and a clean model cache;
3. run the notebook top-to-bottom without editing implementation cells, with every form field at its default (`USE_BYOD_IMAGE = False`, `USE_BYOD_DATASET = False`, `PER_CLASS = 200`, `EPOCHS = 5`, `FREEZE_BACKBONE = False`);
4. verify that Section 1 reports `NOTEBOOK_SOURCE.repository_revision` equal to `metadata.dimer.generated_from.revision`, and that the installed versions equal the inline `PINS`;
5. verify that every default-path stage completes: the pinned install; the carried modules; staging of all three manifest entries and `verify_snapshot`; the sample archive download with its size and SHA-256 check; `validate_dataset`; the three-way split with its leakage assertion; `validate_inputs` with the `top_k=0` rejection finding; ImageNet top-5 predictions and the `sample-sanity` report; the zero-shot baseline; the blank and noise probes; the untrained-head baseline; `finetune`; the held-out comparison; the unseen split; adapter export, reload and the tolerance check; and the five outputs in `outputs/`;
6. record the number of images per class read from the archive, the held-out accuracy and balanced accuracy of the majority-class baseline, the zero-shot mapping, the untrained head and the fine-tuned model, the unseen-split scores, the per-epoch losses, the trainable and total parameter counts, the degenerate-probe answers and the wall time in the table below. No metric value is asserted in advance: a fine-tuned score at or below the zero-shot baseline is a finding to record, not a failure by itself;
7. exercise the BYOD dataset branch once with a small directory or `.zip` of class folders (`BYOD_DATASET_PATH`) and once with an incompatible one (for example a class with a single image), and record both outcomes (REL12);
8. record the notebook Git blob id, commit, runtime (platform, GPU, Python, PyTorch, torchvision, timm), model identifier and revision, the sample archive's SHA-256, whether the model cache was clean, and any warning judged harmless with the reason;
9. record no access tokens or other secrets.

A known-failing default path in the supported runtime blocks release.

## Recorded executions

Notebook identity is the Git blob id of `tutorials/maxvit_classification_colab.ipynb` (verify with `git rev-parse <commit>:tutorials/maxvit_classification_colab.ipynb`). Each record uses the fields Date, Subject, Runtime, Procedure, Observed result and Caveats.

| Date (UTC) | Subject (commit / notebook blob) | Runtime | Procedure | Observed result | Caveats |
|---|---|---|---|---|---|
| 2026-09-26 (kernel 00:24:17 – 00:29:44) | commit `0c1164e` / blob `b736baab41ad` (`NOTEBOOK_SOURCE.repository_revision` `6b59382`, the source revision the notebook was generated from, equal to `metadata.dimer.generated_from.revision`; `6b59382..0c1164e` changes only the notebook and `tests/test_pin_snapshot.py`) | Kaggle Tesla T4 (`kurtvalcorza/dimer-nb2-maxvit-classification` v1), batch run; image `gcr.io/kaggle-gpu-images/python@sha256:37c64f7dd9c54116ecd1bcc88817c5469b88387388fade02bfa8bf3fc647d461` (image torch 2.10.0+cu128), Python 3.12.13, Tesla T4 15360 MiB, driver 580.159.04; after the inline pins torch 2.14.0+cu130 (CUDA 13.0), torchvision 0.29.0+cu130, timm 1.0.29, numpy 2.5.3, Pillow 11.3.0 (equal to `PINS`); device `cuda:0`, float32 | serial suite: blob fetched at the 40-char SHA and Git-blob verified, model cache clean at start, no repository checkout; top-to-bottom with every form field at its default (`USE_BYOD_IMAGE = False`, `USE_BYOD_DATASET = False`, `PER_CLASS = 200`, `EPOCHS = 5`, `FREEZE_BACKBONE = False`, `SEED = 0`, `TOP_K = 5`); `timm/maxvit_tiny_tf_224.in1k` at `041f2cce4d74c7539d63aa9fb85786e78072d487` (apache-2.0), 3 manifest files (123,940,709 B, including `model.safetensors` 123,917,994 B) staged and `verify_snapshot` passed (`verified_files` 3); sample archive `Cleanlab/cifar-10-subset` @ `bb5a7aabf1d1` (mit), 986,707 B, sha256 `66f90a4f87d865e8eb653b62f10e754684075a32314177de76832349d4b1fb19`, fetched and verified | **PASSED (default path)** — 322.8 s wall (pass 1 248.7 s stopped after the install cell with `RuntimeError: Core dependencies changed while older modules were loaded` (cuda-bindings 12.9.4 → 13.4.3, numpy 2.0.2 → 2.5.3), kernel restarted after the install cell as the notebook instructs; pass 2 74.0 s), 14/14 post-restart code cells ok. `validate_dataset`: 400 records, 200 images per class (`frog`, `truck`), 32×32 px, accepted with the finding of **100 groups of pixel-identical images**. Split (seed 0): train 280 / held-out 60 / unseen 60, train majority `frog`, ID leakage assertion passed. `validate_inputs` accepted the 4-image sample with the `top_k=0` rejection finding. ImageNet top-5 on the 4 sample images: frogs → `tailed frog` 0.177 and `ocarina` 0.302 (`tailed frog` third at 0.115), trucks → `moving van` 0.642 and 0.718; `sample-sanity` group hit rate top-1 0.75, top-5 1.0. Held-out (60 images, 30 per class) accuracy / balanced accuracy: majority class 0.500 / 0.500; zero-shot ImageNet mapping 1.000 / 1.000 (confusion [[30, 0], [0, 30]], mean group mass 0.4214); untrained two-class head 0.3667 / 0.3667 (recall frog 0.5667, truck 0.1667); fine-tuned 1.000 / 1.000 (confusion [[30, 0], [0, 30]], mean top-1 score 0.9986). Unseen (60 images): fine-tuned 1.000 / 1.000, mean top-1 score 0.9980. Degenerate probes, ImageNet head: blank → `wing` 0.001, `nematode` 0.001, `sandbar` 0.001; noise → `kite` 0.126, `bald eagle` 0.063, `black stork` 0.053; **adapted head: blank → `frog` 0.828, noise → `truck` 0.756**. `finetune`: full model, 30,404,554 of 30,404,554 parameters trainable, head `head.fc.` reinitialised, 5 epochs, batch 16, AdamW lr 0.0001, weight decay 0.01, no augmentation; epoch losses 0.3225, 0.0256, 0.0051, 0.0019, 0.0012; fine-tune cell about 32 s. Adapter `maxvit_adapter.safetensors` 121,870,424 B, sha256 `7f67837de80a…`, 560 tensors, base state digest `e3998ec8e5f7…`; reload check 60 images, tolerance 0.0001, equivalent. BYOD refusals shown as expected (non-image object → `TypeError`; class with one image → `ValueError`). Five outputs written, sha256 `maxvit_classification_result.json` `abd6fbe9f434…`, `…_evaluation_report.json` `c7cd037465b0…`, `…_input_manifest.json` `3208dccec0e3…`, `…_predictions.csv` `f8530ba8d213…`, adapter `7f67837de80a…` | One seeded split of 60 held-out and 60 unseen CIFAR-10 thumbnails (32 px, upsampled to 224 px), one runtime, one pass, no dispersion estimate. **The zero-shot ImageNet mapping already scores 1.000 on held-out, equal to the fine-tuned head**, so this run shows no gain from fine-tuning; frog versus truck is at ceiling here. The untrained two-class head (0.3667) is below the majority baseline, as expected for a random head. **Near-duplicate leakage:** the archive holds 100 pixel-identical groups (an `original_images` file and its same-numbered `darkened_images` file) and `split_dataset` splits by ID only, so held-out and unseen images can have exact copies in the training split: by the IDs in `maxvit_classification_predictions.csv`, 46 of 60 held-out and 36 of 60 unseen images have their same-numbered counterpart (`original_images` ↔ `darkened_images`) outside every evaluation split, that is, in train (a count of counterparts, not of confirmed pixel copies); the held-out and unseen scores are therefore not evidence of generalisation. The adapted head has no reject option: it put 0.828 on `frog` for a blank image and 0.756 on `truck` for noise. One frog sample image got the non-frog ImageNet top-1 label `ocarina`. The pip resolver's conflict warnings for preinstalled Kaggle packages were harmless (the notebook's version check passed after the restart). **REL12 not exercised** (step 7: BYOD dataset branch with a small class-folder directory and with an incompatible one), so the status stays `Candidate`. No tokens or secrets recorded. |
